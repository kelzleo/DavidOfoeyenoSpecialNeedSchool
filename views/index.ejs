<!-- views/index.ejs -->
<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs', { 
      title: 'David Ofoeyeno Special School - Warri, Delta State', 
      description: 'Welcome to David Ofoeyeno Special School in Warri, Delta State, a dedicated institution for children with autism and neurological disorders, offering inclusive education and support.' 
  }) %>

  <body>
    <%- include('./partials/nav.ejs') %>

    <section class="hero">
      <img 
          class="hero-img" 
          src="https://storage.googleapis.com/<%= bucketName %>/direct/b41.jpg" 
          alt="David Ofoeyeno Special School in Warri, Delta State"
      />
      <h1 class="hero-heading">
        <span>Disability</span>
        <span>Is not</span>
        <span>Inability</span>
      </h1>
      <div class="wave-container">
        <svg viewBox="0 0 500 150" preserveAspectRatio="none">
          <path class="wave" d="M0.00,49.98 C150.00,150.00 349.98,-50.00 500.00,49.98 L500.00,150.00 L0.00,150.00 Z"></path>
        </svg>
      </div>
    </section>

    <div class="index-first"></div>

    <div class="index-second">
      <h2>Welcome to David Ofoeyeno Special School</h2>
      <p>
        Welcome to <a href="/">David Ofoeyeno Special School</a>, located in Warri, Delta State, where every child’s journey is met with compassion and excellence. Our school is a beacon of hope for children with autism and neurological disorders, offering a nurturing environment for growth.
      </p>
      <p>
        Founded on inclusivity and empowerment, we honor Comrade David Ofoeyeno’s vision of resilience and education. Our school is a community dedicated to unlocking every child’s potential through specialized programs. Explore our <a href="/academics">academic programs</a> and <a href="/activity">activities</a>.
      </p>
      <p>
        Our mission is to provide an inclusive environment where students thrive academically, socially, and emotionally. We foster acceptance and respect, celebrating each child’s unique abilities. Learn more about our <a href="/about">mission</a> and <a href="/staffs">dedicated staff</a>.
      </p>
    </div>

    <div class="index-neuro">
      <div class="index-g">
        <h3>Our Vision</h3>
        <p>
          At <a href="/">David Ofoeyeno Special School</a>, we adopt a holistic approach to special needs education, using individualized learning plans and evidence-based practices to empower students in Warri, Delta State, to reach their full potential.
        </p>
      </div>
      <div class="index-g">
        <img 
            class="index-neuropix1" 
            src="https://storage.googleapis.com/<%= bucketName %>/direct/s9.jpg" 
            alt="Special needs education at David Ofoeyeno Special School"
        />
      </div>
    </div>

    <div class="kelz-images">
      <div class="kelz-home">
        <img 
            class="kelz-2home" 
            src="https://storage.googleapis.com/<%= bucketName %>/direct/b23.jpg" 
            alt="Autism event at David Ofoeyeno Special School"
        >
      </div>
      <div class="kelz-home">
        <img 
            class="kelz-2home" 
            src="https://storage.googleapis.com/<%= bucketName %>/direct/s1.jpg" 
            alt="Student activity at David Ofoeyeno Special School"
        >
      </div>
      <div class="kelz-home">
        <img 
            class="kelz-2home" 
            src="https://storage.googleapis.com/<%= bucketName %>/direct/s3.jpg" 
            alt="Special needs program at David Ofoeyeno Special School"
        >
      </div>
      <div class="kelz-home">
        <img 
            class="kelz-2home" 
            src="https://storage.googleapis.com/<%= bucketName %>/direct/s4.jpg" 
            alt="Inclusive education at David Ofoeyeno Special School"
        >
      </div>
    </div>

    <div class="kelz2-images">
      <div class="kelz-11">
        <img 
            class="kelz-22" 
            src="https://storage.googleapis.com/<%= bucketName %>/direct/t1.jpg" 
            alt="Teacher supporting students at David Ofoeyeno Special School"
        >
      </div>
      <div class="kelz-11">
        <img 
            class="kelz-22" 
            src="https://storage.googleapis.com/<%= bucketName %>/direct/s2.jpg" 
            alt="Student support at David Ofoeyeno Special School"
        >
      </div>
      <div class="kelz-11">
        <img 
            class="kelz-22" 
            src="https://storage.googleapis.com/<%= bucketName %>/direct/A11.jpg" 
            alt="Academic program at David Ofoeyeno Special School"
        >
      </div>
      <div class="kelz-11">
        <img 
            class="kelz-22" 
            src="https://storage.googleapis.com/<%= bucketName %>/direct/st1.jpg" 
            alt="Staff member at David Ofoeyeno Special School"
        >
      </div>
    </div>

    <!-- Activity Posts Section -->
    <div class="container">
      <section class="latest-posts">
        <a href="/activity" style="text-decoration: none;">
          <h1>Activities</h1>
        </a>
        <% if (activityPosts && activityPosts.length) { %>
          <div class="posts-grid">
            <% activityPosts.forEach(post => { %>
              <article class="card">
                <a href="/activity/<%= post.slug %>" class="card-link">
                  <div class="card-body">
                    <h4 class="class-title"><%= post.title %></h4>
                    <div class="sub-title"><%= post.createdAt.toLocaleDateString() %></div>
                    <% if (post.imagePath) { %>
                      <div class="image-container">
                        <img 
                            src="https://storage.googleapis.com/<%= bucketName %>/<%= post.imagePath %>" 
                            alt="Activity: <%= post.title %> at David Ofoeyeno Special School"
                            class="article-image"
                            onerror="this.onerror=null; this.src='/images/placeholder.jpg';"
                        >
                      </div>
                    <% } %>
                    <div class="class-text"><%= post.description %></div>
                  </div>
                </a>
                <% if (user && user.isAdmin) { %>
                  <div class="btn-flex">
                    <a href="/activity/edit/<%= post.id %>" class="Edit">Edit</a>
                    <form action="/activity/<%= post.id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
                      <button type="submit" class="btn-danger">Delete</button>
                    </form>
                  </div>
                <% } %>
              </article>
            <% }) %>
          </div>
        <% } else { %>
          <p>No activity posts available yet.</p>
        <% } %>
      </section>
    </div>

    <!-- About Posts Section -->
    <div class="container">
      <section class="latest-posts">
        <a href="/about" style="text-decoration: none;">
          <h3>About Us</h3>
        </a>
        <% if (aboutPosts && aboutPosts.length) { %>
          <div class="posts-grid">
            <% aboutPosts.forEach(post => { %>
              <article class="card">
                <a href="/about/<%= post.slug %>" class="card-link">
                  <div class="card-body">
                    <h4 class="class-title"><%= post.title %></h4>
                    <div class="sub-title"><%= post.createdAt.toLocaleDateString() %></div>
                    <% if (post.imagePath) { %>
                      <div class="image-container">
                        <img 
                            src="https://storage.googleapis.com/<%= bucketName %>/<%= post.imagePath %>" 
                            alt="About: <%= post.title %> at David Ofoeyeno Special School"
                            class="article-image"
                            onerror="this.onerror=null; this.src='/images/placeholder.jpg';"
                        >
                      </div>
                    <% } %>
                    <div class="class-text"><%= post.description %></div>
                  </div>
                </a>
                <% if (user && user.isAdmin) { %>
                  <div class="btn-flex">
                    <a href="/about/edit/<%= post.id %>" class="Edit">Edit</a>
                    <form action="/about/<%= post.id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
                      <button type="submit" class="btn-danger">Delete</button>
                    </form>
                  </div>
                <% } %>
              </article>
            <% }) %>
          </div>
        <% } else { %>
          <p>No about posts available yet.</p>
        <% } %>
      </section>
    </div>

    <!-- Academics Posts Section -->
    <div class="container">
      <section class="latest-posts">
        <a href="/academics" style="text-decoration: none;">
          <h3>Academics</h3>
        </a>
        <% if (academicsPosts && academicsPosts.length) { %>
          <div class="posts-grid">
            <% academicsPosts.forEach(post => { %>
              <article class="card">
                <a href="/academics/<%= post.slug %>" class="card-link">
                  <div class="card-body">
                    <h4 class="class-title"><%= post.title %></h4>
                    <div class="sub-title"><%= post.createdAt.toLocaleDateString() %></div>
                    <% if (post.imagePath) { %>
                      <div class="image-container">
                        <img 
                            src="https://storage.googleapis.com/<%= bucketName %>/<%= post.imagePath %>" 
                            alt="Academic program: <%= post.title %> at David Ofoeyeno Special School"
                            class="article-image"
                            onerror="this.onerror=null; this.src='/images/placeholder.jpg';"
                        >
                      </div>
                    <% } %>
                    <div class="class-text"><%= post.description %></div>
                  </div>
                </a>
                <% if (user && user.isAdmin) { %>
                  <div class="btn-flex">
                    <a href="/academics/edit/<%= post.id %>" class="Edit">.Edit</a>
                    <form action="/academics/<%= post.id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
                      <button type="submit" class="btn-danger">Delete</button>
                    </form>
                  </div>
                <% } %>
              </article>
            <% }) %>
          </div>
        <% } else { %>
          <p>No academics posts available yet.</p>
        <% } %>
      </section>
    </div>

    <!-- Autism Posts Section -->
    <div class="container">
      <section class="latest-posts">
        <a href="/autism" style="text-decoration: none;">
          <h3>Autism</h3>
        </a>
        <% if (autismPosts && autismPosts.length) { %>
          <div class="posts-grid">
            <% autismPosts.forEach(post => { %>
              <article class="card">
                <a href="/autism/<%= post.slug %>" class="card-link">
                  <div class="card-body">
                    <h4 class="class-title"><%= post.title %></h4>
                    <div class="sub-title"><%= post.createdAt.toLocaleDateString() %></div>
                    <% if (post.imagePath) { %>
                      <div class="image-container">
                        <img 
                            src="https://storage.googleapis.com/<%= bucketName %>/<%= post.imagePath %>" 
                            alt="Autism event: <%= post.title %> at David Ofoeyeno Special School"
                            class="article-image"
                            onerror="this.onerror=null; this.src='/images/placeholder.jpg';"
                        >
                      </div>
                    <% } %>
                    <div class="class-text"><%= post.description %></div>
                  </div>
                </a>
                <% if (user && user.isAdmin) { %>
                  <div class="btn-flex">
                    <a href="/autism/edit/<%= post.id %>" class="Edit">Edit</a>
                    <form action="/autism/<%= post.id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
                      <button type="submit" class="btn-danger">Delete</button>
                    </form>
                  </div>
                <% } %>
              </article>
            <% }) %>
          </div>
        <% } else { %>
          <p>No autism posts available yet.</p>
        <% } %>
      </section>
    </div>

    <section class="socials-section">
      <h3>Social Media</h3>
      <h5>Follow Us on Social Media!</h5>
      <div class="socials-container">
        <div class="social-item">
          <a href="https://web.facebook.com/p/Comrade-David-Ofoeyeno-School-For-Special-Children-100048106376007/?_rdc=1&_rdr" target="_blank" rel="noopener">
            <div class="social-bg">Fb</div>
            <div class="social-name">Facebook</div>
          </a>
        </div>
        <div class="social-item">
          <a href="mailto:ofoeyenoautismschool@yahoo.com">
            <div class="social-bg">📧</div>
            <div class="social-name">Email</div>
          </a>
        </div>
        <div class="social-item">
          <a href="tel:+2348162144476">
            <div class="social-bg">📞</div>
            <div class="social-name">Call Us</div>
          </a>
        </div>
      </div>
    </section>

    <!-- Schema Markup for WebPage -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "WebPage",
        "name": "David Ofoeyeno Special School",
        "url": "https://davidofoeyenospecialschool.com",
        "description": "David Ofoeyeno Special School in Warri, Delta State, provides inclusive education and support for children with autism and neurological disorders.",
        "publisher": {
            "@type": "Organization",
            "name": "David Ofoeyeno Special School",
            "logo": {
                "@type": "ImageObject",
                "url": "https://storage.googleapis.com/<%= bucketName %>/direct/newlogo.jpg"
            }
        },
        "address": {
            "@type": "PostalAddress",
            "streetAddress": "Comrade David Ofoeyeno School, Oputu Lay-out off Port-Harcourt Drive Dsc Township Orhuwhorun, Behind Challenge Academy",
            "addressLocality": "Warri",
            "addressRegion": "Delta State",
            "postalCode": "320242",
            "addressCountry": "Nigeria"
        }
    }
    </script>

    <%- include('./partials/footer.ejs') %>

    <script>
      function toggleMobileMenu(menu) {
        var isOpen = menu.classList.toggle('open');
        if (!isOpen) {
          closeDropdowns();
        }
      }
      
      function toggleDropdown(event) {
        event.stopPropagation();
        var parentListItem = event.target.parentElement;
        parentListItem.classList.toggle('open');
      }
      
      function closeDropdowns() {
        var dropdowns = document.querySelectorAll('.has-dropdown');
        dropdowns.forEach(function(dropdown) {
          dropdown.classList.remove('open');
        });
      }
      
      document.addEventListener('click', function(event) {
        var dropdowns = document.querySelectorAll('.has-dropdown');
        dropdowns.forEach(function(dropdown) {
          if (!dropdown.contains(event.target)) {
            dropdown.classList.remove('open');
          }
        });
      });
      
      document.addEventListener('resize', function() {
        closeDropdowns();
      });
      
      document.getElementById('ham-icon').addEventListener('click', function(event) {
        if (!event.currentTarget.classList.contains('open')) {
          closeDropdowns();
        }
      });
      
      document.addEventListener("DOMContentLoaded", function () {
        const items = document.querySelectorAll(".kelz-home");
        const observer = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add("visible");
            } else {
              entry.target.classList.remove("visible");
            }
          });
        }, { threshold: 0.1 });
        items.forEach(item => observer.observe(item));
      });

      document.addEventListener('DOMContentLoaded', function () {
        const images = document.querySelectorAll('.kelz-22');
        const observerOptions = {
          root: null,
          rootMargin: '0px',
          threshold: 0.1
        };
        const observer = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('in-view');
            } else {
              entry.target.classList.remove('in-view');
            }
          });
        }, observerOptions);
        images.forEach(image => observer.observe(image));
      });
    </script>
  </body>
</html>